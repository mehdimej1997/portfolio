import React from "react";
import { SectionTitle } from "./SectionTitle";

type Props = {};

export const PlayGround = (props: Props) => {
  return (
    <div className="container mx-auto">
      <div>
        <SectionTitle titleOne="Playground" titleTwo="" />
      </div>
    </div>
  );
};
